extends layout

block content
  .container.mt-5
    h1 #{title}
    form(method='POST', action='/multiplicar/tabla-multiplicar')
      .form-group
        label(for='numeroBase') Numero Base
        input#numeroBase.form-control(type='number', name='base', required='required')
      button.btn.btn-primary(type='submit') Calcular
